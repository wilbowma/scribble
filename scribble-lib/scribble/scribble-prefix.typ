// The default Scribble prefix for Typst output; use the `--prefix`
// flag (or `#:prefix-file` argument) to substitute a different prefix.
#set page(numbering: "1")
#set par(justify: true)
#set page(margin: (right: 1.2in, left: 1.2in))
